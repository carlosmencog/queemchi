{
  "name": "queemchi",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev --host 127.0.0.1 --port 3000",
    "build": "astro build",
    "preview": "astro preview --host 127.0.0.1 --port 3000"
  },
  "dependencies": {
    "astro": "^5.9.3"
  }
}
